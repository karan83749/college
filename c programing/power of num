#include<stdio.h>
int main()
{
    int a,b,y=1,i;
    printf("enter the number and its power : ");
    scanf("%d%d",&a,&b);
    for(i=1;i<=b;i++)
    y=y*a;
    printf("result is %d",y);
    return 0;
}